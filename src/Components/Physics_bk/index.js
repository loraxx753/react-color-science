import React from 'react'
import Forces from './Forces'
import { Heading } from 'Components/Primitives'

export default props => (
  <React.Fragment>
    <Heading>Physics Index</Heading>
    <Forces />
  </React.Fragment>
)

export { Forces }
